{
  "name": "ftnmdreader",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for ftnMDReader's local CodeMirror bundle. The app itself needs no build step to run — just open index.html. This is only needed if you edit app.src.js and want to regenerate app.js.",
  "scripts": {
    "build": "esbuild app.src.js --bundle --format=iife --outfile=app.js --target=es2020 --minify"
  },
  "dependencies": {
    "@codemirror/commands": "^6.11.0",
    "@codemirror/lang-markdown": "^6.5.2",
    "@codemirror/language": "^6.12.4",
    "@codemirror/search": "^6.7.2",
    "@codemirror/state": "^6.7.4",
    "@codemirror/view": "^6.43.11",
    "@lezer/highlight": "^1.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
